# Loosened pins for Python 3.14 compatibility on Streamlit Cloud.
# Critical pin: pandas < 3.0 to avoid the groupby() API breakage we hit.
# Everything else: latest stable, the code is broadly compatible.
streamlit
pandas<3.0
numpy
google-cloud-bigquery
google-auth
pyarrow
db-dtypes
altair
# Distribution tab: interactive (zoom/pan/click) merchandising-zone map.
plotly
# Weather tab: NWS-style filled-contour maps (interpolation + render).
matplotlib
scipy
# Sell-Through tab: buyer-facing Excel export (optional — tab degrades to
# CSV-only if missing).
xlsxwriter
